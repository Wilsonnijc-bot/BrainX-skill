'use strict';

const PACKAGE_NAME = 'brainx-skills';
const RECEIPT_SCHEMA_VERSION = 1;
const SKILL_NAME_PATTERN = /^[a-z0-9]+(?:-[a-z0-9]+)*$/;

module.exports = {
  PACKAGE_NAME,
  RECEIPT_SCHEMA_VERSION,
  SKILL_NAME_PATTERN,
};
